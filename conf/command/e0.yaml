mode: evaluate
name: filter_surrogate_fm

networks:
  - vision_network
  - filtering_network

filtering_network:
  architecture:
    normalization: 'cn'
    feature_modulation: true
  train: false
    
codec:
  ds_levels: [0]
  name: vvenc
  qualities: [50, 45, 40, 35, 30, 25]

control_input: true

target_path: ???
target_step: 10000